# Cat runtime parts

Phaser가 같은 위치에 겹쳐 조립하는 320×320 SVG 파츠와 `cat-rig.json`을 둡니다. 기본 순서는
`back-leg`, `tail`, `body`, `front-leg`, `head`, `face`입니다. `assembled-preview.svg`는 검수용이며
애니메이션 런타임에서는 개별 파츠를 사용합니다. 편집 원본은 `source-assets/svg/characters/cat/`입니다.
