# Stage 1 runtime audio

Phaser와 브라우저에서 접근하는 MP3 복제본입니다. 편집 가능한 원본은
`source-assets/audio/raw`에 있으며 이 디렉터리의 파일은 `stage001AudioManifest.ts` marker로 일부 구간만
재생합니다. 원본 출처와 라이선스 메타데이터는 `docs/audio/STAGE_001_AUDIO.md`에서 관리합니다.
